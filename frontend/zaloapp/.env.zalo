# Chỉ dùng khi build `--mode zalo` (zmp deploy). Zalo Mini App chạy trên origin
# của Zalo (h5.zdn.vn) → phải gọi backend + map proxy bằng URL TUYỆT ĐỐI tới EC2.
VITE_API_BASE_URL=https://56-10-45-176.sslip.io
